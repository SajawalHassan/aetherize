import { Editor } from "./_components/editor";

type Props = {};

const Page = (props: Props) => {
  return (
    <div>
      <Editor />
    </div>
  );
};

export default Page;

export default Page;
